# Never commit a real API key.
OPENAI_API_KEY=

# Set this to a model identifier available to your account.
# The project intentionally does not hardcode a provider model alias.
AUTOFORMULATION_MODEL=
